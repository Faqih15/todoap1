import React from "react";

export default function Tabel(peyek) {
  return (
    <div>
      <Card
        key={idx}
        bgColor={warna[data.warna % warna.length]}
        ubah={ubah}
        idx={idx}
        data={data}
      />
    </div>
  );
}
